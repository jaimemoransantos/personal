/**
 * Default organization ID used when no multi-tenant context exists yet.
 * In a single-tenant setup (e.g. Geomtech only), all users belong to this org.
 */
export const DEFAULT_ORGANIZATION_ID = "default";
